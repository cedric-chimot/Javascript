/*let multi = (x, y) => {return x*y};
document.write(multi(47, 12));*/

let modulo = (a, b) => 

    {
        if (a%b != 0) 

        {
            return ok;
        }

        else {

            return pas_ok;

        }

        }

